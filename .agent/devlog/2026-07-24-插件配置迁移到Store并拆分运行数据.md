# 2026-07-24 插件配置迁移到 Store 并拆分运行数据

## Session goal
建立 `plugin_config` / `plugin_storage` 两类持久化接口，并删除已被定时提醒取代的独立喝水、护眼能力。

## Completed
- rest、timer、agent 和外部插件启用配置统一进入 Tauri Store。
- SQLite 新增真实 `plugin_storage` 表，timer 触发游标与外部插件业务数据写入该表。
- 删除 `water_records`、独立喝水/护眼后端、设置卡、Toast 卡和 Dashboard 水统计。
- Rust 后台直接通过 StoreExt 读取用户配置。

## Pending
- 非插件的应用级通用设置仍使用现有 SQLite `settings`，后续按领域逐项判断，不在本轮批量迁移。

## Key file changes
| File | Change |
|---|---|
| `src-tauri/src/plugin_config.rs` | Store 命名空间与 typed/entry 读写 |
| `src-tauri/src/db.rs` | `plugin_storage` 表及隔离读写 |
| `src-tauri/src/timer_plugin.rs` | 配置与运行游标拆分 |
| `src-tauri/src/agent_hook.rs` | Agent 用户设置迁移到 Store |
| `src-tauri/src/lib.rs` | 移除独立喝水/护眼运行路径 |
