# 2026-08-03 Step4 S4.1 本地插件安装

## Session goal

Step 4 首选切片：插件中心从文件夹 / zip 安装到 `plugins/`。

## Completed

- 路线图 `step4-roadmap-plugin-ecosystem.md`
- Rust `install_external_plugin`（manifest 校验、zip 防穿越、覆盖门闩、rescan）
- UI：安装文件夹 / zip / 打开目录；覆盖 confirm
- `cargo check` + `vue-tsc` 通过

## Pending

- 真机手测文件夹与 zip
- S4.2 打包约定文档

## Key files

| File | Change |
|------|--------|
| `src-tauri/src/plugins.rs` | install command |
| `src-tauri/Cargo.toml` | `zip` crate |
| `PluginNavRail.vue` / `Plugins.vue` | install UX |
| `src/api/tauri.ts` | pick + install API |
