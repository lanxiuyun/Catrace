# 2026-08-27 Toast 小窗化与点击抢焦点修复

## 会话目标

完成 `feat-small-toast-window` 分支的知识沉淀，覆盖该分支相对 `main` 的全部改动：Toast 从全屏覆盖层改回右下角原生小窗、小窗增高闪屏修复、点击卡片抢焦点实现及 capability 修复。

## 分支关键 commit

| commit | 说明 |
|--------|------|
| `7effbb5` `1` | Toast 小窗化：去掉全屏覆盖/点击穿透/命中轮询，改为右下角小窗 + 内容尺寸上报 |
| `ed6b2f8` `Update plugin-demo` | 插件 demo submodule 指针更新 |
| `5dd33fe` | 修复小窗增高时卡片闪到任务栏：两步 `SetWindowPos` + 前端贴顶布局 |
| `85c86bb` | 点 Toast 临时抢焦点：`pointerdown` → `setWindowActiveMode(true)` |
| `3afe019` | 修复抢焦点未生效：plugin 命令提升为 app command，Windows 用 `AttachThreadInput` 抢焦点 |

## 已完成知识归档

### Bug 记录
- `.agent/bugs/2026-08-27-toast小窗增高时卡片闪到任务栏.md`
- `.agent/bugs/2026-08-27-toast点击卡片无法抢焦点因inline-plugin命令未授权capability.md`

### 特性文档
- `.agent/features/toast-window/toast小窗化实现-右下角定位-内容尺寸上报-与去穿透.md`
- `.agent/features/toast-window/README.md`（已更新：小窗特性、点击抢焦点说明）
- `.agent/features/toast-window/full-screen-click-through-overlay-windows-implementation.md`（已标记废弃）
- `.agent/features/window-manager/README.md`（已更新：前端 API、聚焦流程）
- `.agent/architecture/window-manager/README.md`（已更新：app command 注册、capability 踩坑）
- `.agent/architecture/desktop-event-os/toast-renders-only-from-event-bus.md`（已更新：小窗描述）

### 开发日志
- 本文档

## 关键状态机

| 状态 | 触发条件 | 键盘焦点 |
|------|---------|---------|
| 让路 Passthrough | 光标不在卡片上 | 在原窗口 |
| 可点 Hoverable | 光标在卡片上（滑入） | 仍在原窗口 |
| 接管 Engaged | 点了卡片 | Toast 拿到焦点 |

## 关键文件变更

| 文件 | 变更 |
|------|------|
| `src-tauri/src/reminder_toast.rs` | 小窗化：`fit_toast_window`、`set_toast_content_size`、移除命中轮询 |
| `src-tauri/src/window_manager/windows.rs` | 去掉 subclass/穿透；新增两步 resize；新增 `force_foreground_window` |
| `src-tauri/src/window_manager/mod.rs` | `set_window_active_mode` 改为 app command |
| `src-tauri/src/lib.rs` | 注册 `set_window_active_mode` |
| `src/views/toastWindows/ReminderToast.vue` | 去掉 hit region；新增 `reportWindowSize`；`pointerdown` 抢焦点；贴顶布局 |
| `src/api/tauri.ts` | `setToastHitRegions` → `setToastContentSize`；invoke 路径改 app command |
| `src/views/toastWindows/ToastShell.vue` | 强制透明背景 |

## 验证状态

- `cargo check` 通过
- `pnpm vue-tsc --noEmit` 通过
- 用户实测：Toast 点击后可正常复制，焦点切换到 Toast
