# 2026-07-22 主界面顶部导航与覆盖式滚动条重构

## Session goal

参考 `catrace.html` 将普通应用页面从左侧主导航重构为顶部导航，并让插件页保持左侧二级栏与右侧工作区。

## Completed

- 普通页面改为固定顶部导航，Reminder 独立窗口继续绕过普通应用外壳。
- 插件页保持左右分栏，左侧升级为带数量、搜索、状态圆点和本地插件入口的插件中心。
- 内置插件详情统一为固定顶部标题/总开关，下面的配置内容独立滚动。
- 新增覆盖式滚动容器：原生滚动条隐藏，悬停或拖动时显示浮层滑块，不占内容宽度。
- 覆盖式滚动样式只接入普通应用和插件区域，避免影响 Toast 的尺寸测量与透明窗口。
- 全局顶栏收紧到 `3rem`，交流群以弱强调 Tag 放在应用级顶栏右侧，不占插件侧栏空间。

## Key file changes

| File | Change |
|------|--------|
| `src/App.vue` | `3rem` 顶部导航、应用级交流群 Tag、普通应用外壳与主工作区滚动容器 |
| `src/components/OverlayScrollbar.vue` | 覆盖式滚动条尺寸同步、悬停显示和拖动逻辑 |
| `src/views/Plugins.vue` | 插件中心搜索/状态/本地入口，以及固定左右分栏和独立滚动边界 |
| `src/components/plugins/RestPluginPanel.vue` | 固定插件顶栏，配置区使用覆盖式滚动 |
| `src/components/plugins/TimerPluginPanel.vue` | 固定插件顶栏，规则区使用覆盖式滚动 |
| `src/components/plugins/AgentPluginPanel.vue` | 固定插件顶栏，设置区使用覆盖式滚动 |

## Key conventions

- Reminder Popup、Fullscreen、Toast 不挂载 `.app-shell`，普通页面样式必须限定在该外壳内。
- 页面滚动优先使用 `OverlayScrollbar`，不要再给同一容器添加原生 `overflow-y: auto`。
- 插件页窄窗口仍保持左右分栏，仅收窄插件栏，避免列表和详情变成上下堆叠。
- 插件侧栏只承载插件上下文；应用级信息放在全局顶栏。
- “探索更多插件”在没有插件市场前只打开本地插件目录，不暗示不存在的在线能力。
