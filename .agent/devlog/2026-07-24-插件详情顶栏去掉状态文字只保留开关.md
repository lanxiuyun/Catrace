# 2026-07-24 插件详情顶栏去掉状态文字只保留开关

## Session goal

把插件详情顶栏的「插件状态」文字标签去掉，只保留 switch，并保证开关固定在右侧不换行。

## Completed

- 移除四个插件详情顶栏的状态文字标签：
  - `src/views/Plugins.vue` — 外部插件详情
  - `src/components/plugins/RestPluginPanel.vue` — 久坐提醒
  - `src/components/plugins/TimerPluginPanel.vue` — 定时提醒
  - `src/components/plugins/AgentPluginPanel.vue` — Agent 通知
- 顶栏 `flex-wrap: nowrap`，标题/描述超长时省略，开关 `flex-shrink: 0` 不被挤压。
- 在 `common` i18n 中新增 `enabled` / `disabled` 词条，供后续复用。
- 创建 `e2e-temp/` 目录作为临时 Playwright 测试目录，并加入 `.gitignore`。
- 用 Playwright 连接本地 `pnpm tauri dev`（`http://localhost:1420`）验证开关独立存在且无文字标签。
- 提交 commit：`refactor(plugin): remove status labels and keep switch only`

## Key file changes

| File | Change |
|------|--------|
| `src/views/Plugins.vue` | 外部插件顶栏去掉 `.master-label`，只保留 switch；顶栏不换行 |
| `src/components/plugins/RestPluginPanel.vue` | 去掉状态文字，switch-only |
| `src/components/plugins/TimerPluginPanel.vue` | 同上 |
| `src/components/plugins/AgentPluginPanel.vue` | 同上 |
| `src/i18n/locales/zh-CN.ts` | `common.enabled` / `common.disabled` |
| `src/i18n/locales/en-US.ts` | `common.enabled` / `common.disabled` |
| `.gitignore` | 忽略 `e2e-temp/` |
| `.agent/features/plugin-center/插件状态排序和统一详情顶栏实现约定.md` | 更新顶栏约定 |
