# 2026-07-24 Step3 宿主能力改为随具体插件按需提供

## Session goal

调整 Step 3 后续顺序，避免在没有真实插件场景时提前建设通用宿主能力。

## Completed

- 将 M12 从独立前置里程碑改为按具体插件需求延后。
- 明确 `plugin_open`、前台窗口读取和受限文件写入不要求一次性补齐。
- 明确 napcat 收发与快速回复留到对应消息插件开发时做端到端验证。
- 固化“先选具体插件，再提供最小宿主能力”的开发原则。
- 记录新增能力仍须在 Rust 固定调用方身份、协议、目标、路径或对象所有权边界。

## Pending

- 尚未选择下一个具体插件。
- 等具体插件需求明确后，再决定是否新增宿主 invoke。

## Key file changes

| File | Change |
|------|--------|
| `.agent/architecture/desktop-event-os/step3-roadmap-plugin-runtime.md` | 将 M12 改为按需延后，并更新完成定义和实现顺序 |
| `.agent/decisions/2026-07-24-step3-宿主能力改为随具体插件按需提供.md` | 记录不提前建设通用宿主 API 的架构决策 |
| `.agent/manifest.yaml` | 索引决策与本次开发记录，并更新 `current.next` |
