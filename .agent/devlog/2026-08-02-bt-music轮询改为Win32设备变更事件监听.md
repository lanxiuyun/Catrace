# 2026-08-02 bt-music 轮询改为 Win32 设备变更事件监听

## Session goal

把 bt-music 的定时 `Get-PnpDevice` 轮询改成系统级设备连接变更通知（事件驱动）。

## Completed

- sidecar 去掉 `setInterval` / `pollIntervalMs`；常驻 PowerShell `ManagementEventWatcher` 订阅 `Win32_DeviceChangeEvent`（到达/移除）
- 事件后仍用 `DEVPKEY_Device_IsConnected` + 分组去重做快照 delta；启动 seed 不 publish
- PS 侧 700ms / Node 侧 350ms 合并 PnP 突发；watcher 崩溃 2s 重启；shutdown `taskkill /T`
- settings 去掉「轮询间隔」；文案改为系统级事件
- feature README / windows-pnp 子文档 / plugin-demo README 同步

## Remaining

- 真机：连/断耳机确认 Toast 延迟与进程清理
- M15.3 storage + Plugins UI sidecar 状态

## Key file changes

| File | Change |
|------|--------|
| `tools/plugin-demo/bt-music/runtime/main.mjs` | 事件 watcher + 共享 PnP 采集脚本 |
| `tools/plugin-demo/bt-music/settings.mjs` | 去掉 pollIntervalMs |
| `.agent/features/bt-music/README.md` | 一句话与约定改为事件驱动 |
| `.agent/features/bt-music/windows-pnp-….md` | 监听架构与手测更新 |
| `tools/plugin-demo/README.md` | demo 描述去掉 poll |
