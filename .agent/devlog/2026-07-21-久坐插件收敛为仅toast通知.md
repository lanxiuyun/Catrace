# 2026-07-21 久坐插件收敛为仅 toast 通知

## Session goal

久坐提醒插件暂不支持全屏，只保留通知（toast）提醒。

## Completed

- `RestPluginPanel` 去掉提醒方式 / 全屏配置 UI；挂载时遗留 mode 写回 toast
- `rest_plugin::show_notification` 固定 bus toast，不再读 `reminder_mode`
- `test_notification` 始终发 rest-timer 测试卡
- `get_reminder_mode` / `set_reminder_mode` 非 toast 钳制为 toast
- 更新 boundary / settings 收敛文档 / popup-removal 阶段性决策
- `vue-tsc` 与 `cargo check` 通过（`create_*_window` dead_code 告警为预期）

## Remaining

- 确认无回归需求后，再删 popup/fullscreen 窗口与 i18n 死文案
- 可选：按维度 commit 本轮改动

## Key file changes

| File | Change |
|------|--------|
| `src/components/plugins/RestPluginPanel.vue` | toast-only UI + 迁移 |
| `src-tauri/src/rest_plugin.rs` | 固定 toast；测试 always timer |
| `src-tauri/src/lib.rs` | mode API 钳制 |
| `.agent/decisions/popup-removal-pending.md` | 阶段性状态 |
| `.agent/architecture/.../rest-reminder-builtin-plugin-boundary.md` | toast-only 边界 |
