# 2026-09-20 mac 不催辅助功能授权，横幅和设置项都藏掉

## 会话目标

ad-hoc 签名导致每次更新都要重新勾辅助功能。产品决定：兜底采样够用，不要再提醒。

## 完成项

- `MainShell` 去掉 `AccessibilityBanner`
- 设置卡 `showAccessibility` 恒 false
- 后端 fallback 采样未改

## 待开发

- Developer ID 签名落地后再考虑把授权入口接回设置（仍不必做主窗横幅）

## 关键文件变更

| 文件 | 变更 |
|------|------|
| `src/views/mainWindow/MainShell.vue` | 不挂横幅 |
| `src/components/settings/SystemSettingsCard.vue` | 不展示授权行 |
