# 2026-08-31 更新支持换源，Toast 也能选

## 会话目标

给更新加上换源：设置页和更新窗口都能选 GitHub 镜像。

## 完成项

- SQLite `update_source`：`auto` / `ghfast` / `ghddlc` / `ghproxy` / `github`
- Rust `check_app_update` 覆盖 endpoints，锁定源时改写下载 URL
- 设置页 NSelect；更新 Toast 原生 select
- 启动自动检查读已保存源

## 待开发

- 无

## 关键文件变更

| 文件 | 变更 |
|------|------|
| `src-tauri/src/update.rs` | 源、检查、URL 改写 |
| `src-tauri/src/lib.rs` | 自动检查走已保存源 |
| `src/components/settings/SystemSettingsCard.vue` | 换源 |
| `src/components/UpdateToastCard.vue` | Toast 选源 |
| `src/api/tauri.ts` | `checkAppUpdate` |
