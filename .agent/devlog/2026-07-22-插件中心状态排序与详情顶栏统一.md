# 2026-07-22 插件中心状态排序与详情顶栏统一

## Session goal

完善插件中心的启用状态反馈和插件详情页结构，让左侧导航反映真实状态，右侧插件面板保持一致的基础 Header。

## Completed

- 内置插件状态改为读取各自真实配置，不再无条件显示绿色状态点。
- 插件列表按“已启用优先、名称升序”排序。
- 内置插件开关变化通过 `catrace:plugin-enabled-changed` 通知插件中心，列表实时刷新并重新排序。
- 左侧插件栏固定为 `15rem`，窗口缩小时不被 flex 压缩。
- Rest、Timer、Agent 三个内置插件统一 Header，只保留图标、名称、描述和启用开关。
- 测试通知、添加规则、活动规则数量等插件专属内容移动到下方自定义区域。

## Key file changes

| File | Change |
|------|--------|
| `src/views/Plugins.vue` | 状态读取、排序、实时刷新和固定左栏宽度。 |
| `src/components/plugins/RestPluginPanel.vue` | 统一 Header，测试按钮移入内容区。 |
| `src/components/plugins/TimerPluginPanel.vue` | 统一 Header，添加规则按钮移入内容区。 |
| `src/components/plugins/AgentPluginPanel.vue` | 开关变化通知插件中心。 |
| `.agent/features/plugin-center/` | 记录插件中心的状态与布局约定。 |
