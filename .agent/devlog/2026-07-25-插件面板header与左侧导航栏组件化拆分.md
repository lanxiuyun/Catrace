# 2026-07-25 插件面板 header 与左侧导航栏组件化拆分

## Session goal

响应“将插件面板拆分成上下组件、header 抽离、左侧导航栏也抽离”的需求，把 `Plugins.vue` 和各内置插件面板的重复 header / 导航逻辑拆成可复用组件，并统一外部插件详情页布局。

## Completed

- 新增 `src/components/plugins/PluginPanelHeader.vue`：统一插件详情页 header（图标 slot + 标题/副标题 + 开关）。
- 新增 `src/components/plugins/PluginNavRail.vue`：统一左侧插件导航栏。
- `RestPluginPanel.vue` / `TimerPluginPanel.vue` / `AgentPluginPanel.vue` 复用 `PluginPanelHeader`，删除各自 header 样式。
- `Plugins.vue` 复用 `PluginNavRail`，删除左侧导航模板与样式。
- 外部插件详情页改为 header + 下方占位内容，保留错误提示和测试通知按钮。
- 补充 `plugins.external.switchAria` / `settingsPlaceholder` 中英文文案。
- 修复清理 import 时误删 `NSwitch` 导致运行时警告的问题。

## Key file changes

| File | Change |
|------|--------|
| `src/components/plugins/PluginPanelHeader.vue` | 新增统一 header 组件 |
| `src/components/plugins/PluginNavRail.vue` | 新增左侧导航栏组件 |
| `src/components/plugins/RestPluginPanel.vue` | 复用 header，删除本地 header 样式 |
| `src/components/plugins/TimerPluginPanel.vue` | 复用 header，恢复误删的 `NSwitch` import |
| `src/components/plugins/AgentPluginPanel.vue` | 复用 header，删除本地 header 样式 |
| `src/views/Plugins.vue` | 使用 `PluginNavRail`，简化外部插件详情布局，清理大量样式 |
| `src/i18n/locales/zh-CN.ts` / `en-US.ts` | 新增外部插件开关 aria 与占位文案 |
| `.agent/features/plugin-center/README.md` | 更新文件列表与 sub-docs |
| `.agent/features/plugin-center/插件面板和导航栏组件化拆分.md` | 新增本轮知识沉淀 |
